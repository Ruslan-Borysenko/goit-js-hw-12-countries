const refs = {
  body: document.querySelector('body'),
  searchInput: document.querySelector('input'),
  searchInputClose: document.querySelector('span'),
  ul: document.createElement('ul'),
};

export default refs;
